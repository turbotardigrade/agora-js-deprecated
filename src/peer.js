/**
 * Created by prashanthcr on 31/10/2016.
 */

const ipfsApi = require('ipfs-api')
const ipfs = ipfsApi() // connect to default daemon at localhost:5001


class Peer {
  /**
   * Looks up hardcoded bootstrapped peer list stored in IPFS
   * Initializes local peer store with it and persists it to disk
   */
  getPeers() {

  }
}